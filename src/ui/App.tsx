export function App() {
  return (
    <main className="mx-auto max-w-5xl px-6 py-16">
      <h1 className="text-4xl font-bold tracking-tight">unmark</h1>
      <p className="mt-3 max-w-prose text-[var(--color-muted)]">
        Strip watermarks and provenance marks in your browser. Nothing you give it is uploaded,
        because there is no server to upload it to.
      </p>
    </main>
  )
}
